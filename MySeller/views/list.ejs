<h1 class="display-4">All Products</h1>
<!-- 
<table class="table table-striped table-bordered">
    <thead class="thead-dark">
    <tr>
        <th>ID</th>
        <th>Product ID</th>
        <th>Product Name</th>
        <th>Product Price (CAD)</th>
        <th>Quantity on Hand</th>
        <th></th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    <% prod.forEach(function(product){ %>
        <tr>
            <td><%= product.id %></td>
            <td><%= product.product_id %></td>
            <td><%= product.product_name %></td>
            <td><%= product.product_price %></td>>
            <td><%= product.qoh %></td>
            <td>
                <form class="d-inline" action="/products/delete/?id=<%= product.id %>" method="post" onsubmit="return confirm('Are you sure you want to delete the entry?') ">
                    <input type="Submit" value="Delete" class="btn btn-outline-danger">
                </form>
            </td>
            <td>
                <form class="d-inline" action="/products/edit/?id=<%= product.id %>" method="post">
                    <input type="Submit" value="Edit" class="btn btn-outline-dark">
                </form>
            </td>
        </tr>
    <%  }) %>
    </tbody>
</table> -->

<div class="row ">

    <% prod.forEach(function(product){ %>

    <div class="card border-black m-4" style="width: 18rem;">
        <img class="card-img-top" src="<%= product.product_url%>"
            alt="Card image cap">
        <div class="card-body">
            <p>Name: <%= product.product_name %></p>
            <p>Price: $<%= product.product_price %></p>
            <p>Quantity on Hand: <%= product.qoh %></p>

            <form class="d-inline" action="/products/edit/?id=<%= product.id %>" method="post">
                <input type="Submit" value="Edit" class="btn btn-outline-dark">
            </form>
            <form class="d-inline" action="/products/delete/?id=<%= product.id %>" method="post"
                onsubmit="return confirm('Are you sure you want to delete the entry?') ">
                <input type="Submit" value="Delete" class="btn btn-outline-danger">
            </form>
        </div>
    </div>

    <%  }) %>

</div>